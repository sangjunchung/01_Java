package com.kh.variableEx;

/*
(자료)형변환 -	데이터 타입을 변환하는 것
		    변환에는	자동으로 변환하는 형변환
		          	강제로 변환하는 형변환
		    	     
		    묵시적 형변환(자동 형변환)
			자동으로 형변환이 가능한 경우는 자료형의 byte 가 서로 같거나 
			변환하는 결과가 변환전 byte 보다 클 경우 자동으로 형변환 가능
			 
			명시적 형변환(강제 형변환)
			큰 byte에서 작은 byte로 넘어갈 때는 큰 그릇에서 작은 그릇으로
			이동하기때문에 강제로 넣어줘야 함(강제로 넣을 경우 데이터 손실 발생 가능성 있음)
		   
			메서드 호출을 통한 형변환
			일부 데이터들은 메서드를 통해 값 변환이 진행됨
			문자열 String 으로 담은 값을 숫자계열로 변경하고 싶을 때 많이 사용
			ex. 문자열 -> 정수
			String 핸드폰번호 = "01012345678";
			int phone = Integer.parseInt(핸드폰번호);
*/

public class 형변환 {

}
